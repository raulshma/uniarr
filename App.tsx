/**
 * Expo Router manages the runtime entry point through `expo-router/entry`.
 * This file remains to keep tooling compatibility with the Expo ecosystem.
 */
export default function App() {
  return null;
}
